# React-Fashion-Gallery
An image gallery website deployable via Docker. The frontend is built using Vite + React, while the production environment utilizes Node/Express to serve both static pages and the `/api/*` endpoints; user, session, and visitor application data are stored in an SQLite database.
